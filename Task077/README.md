# Task077 — std::string 🔍

## В чем проблема

У меня есть навык телепатии. И я ощущаю... ощущаю как вам надоело использовать строки в стиле С. 😆

Действитлеьно, многие операции с этим видом строк представляет неудобства. Банальные операции требуют небанальных решений в коде. Все как-то недружественно что-ли. И поэтому в C++ был введен отдельный тип данных std::string! 

## Описание и примеры использования

Класс доступен приподключении библиотеки `<string>`

Сам по себе `string` определен в нэймспэйсе (NS) `std`. Вы либо должны прямо указывать этот NS при использовании типа (предпочтительнее) или воспользоваться инструкцией `using namespace std;` перед использованием и далее писать просто `string`

Тип string содержит последовательность символов типа char, которая может быть пустой. Например, определение пустой строки:
`std::string message;`

Также можно инициализировать или присвоить переменной string конкретную строку:
```c++
std::string message {"Hello world!"};
std::string message2 = "Hello world!";
std::string message3("Hello world!");
```

В данном случае переменная message получит копию строкового литерала "Hello world!". В своем внутреннем представлении переменная message будет хранить массив символов, который также заканчивается на нулевой байт. Однако реализация типа string и предлагаемые им возможности делают работу с этим типом более безопасной.

Есть ряд других способов инициализации. Так, можно инициализировать строку повторяющимся набором символов:
`std::string message (4, 'd'); // повторяем d 4 раза - message = dddd`

И можно инициализировать объект string дргим объектом string:
```c++
std::string hello{"hello world"};
std::string message {hello}; // message = "hello world"
// или так
std::string message1 (hello);
std::string message2 = hello;
```

Можно инициализировать только часть строки
```c++
std::string message1 {"hello world", 4}; // берем первые 4 символа - message1 = hell
std::string message2 {"hello world", 6, 5}; // 5 символов начиная с 6 символа - message2 = world
 
std::string hello{"hello world"};
std::string message3 {hello, 4};            // message3 = hell
std::string message4 {hello, 6, 5};         // message4 = world
```

Мы можем вывести подобную строку на консоль:
```c++
#include <iostream>
#include <string>
 
int main()
{
    std::string message {"Hello world!"};
    std::cout << "Message: " << message << std::endl; // Message: Hello world!
}
```

С помощью стандартных потоков ввода и вывода cin и cout можно ввести данные в строку и вывести на консоль:
```c++
	
#include <iostream>
#include <string>
 
int main()
{   
    std::string name;
    std::cout << "Input your name: ";
    std::cin >> name;
    std::cout << "Your name: " << name << std::endl;
}
```

Консольный вывод:
```
Input your name: Tom
Your name: Tom
```

Для объединения строк доступна операция сложения:
```c++
#include <iostream>
#include <string>
 
int main()
{
    std::string hello {"hello"};
    std::string world {"world"};
    std::string message{ hello + " " + world};
    std::cout <<  message  << std::endl;    // hello world
}
```

Стоит отметить, что при операции сложения оба операнда НЕ должны одновременно представлять строковые литералы. Например, в следующем случае мы получим ошибку
`std::string message{ "hello " + "world" + "!"};   // ! Ошибка`

Если вдруг все таки необходимо объединить два строковых литерала, то можно просто опустить операцию сложения
```c++
std::string hello {"hello"};
std::string message{ hello + " world" "!"};   // hello world!
```

В качестве альтернативы можно неявно преобразовать строковый литерал в объект string. Для этого импортируется пространство имен std::string_literals и к строковым литералам добавляется суффикс s
```c++
#include <iostream>
#include <string>
using namespace std::string_literals; 
 
int main()
{
    std::string message{ "hello "s + "world"s + "!"s};
    std::cout <<  message  << std::endl;    // hello metanit.com
}
```

В данном случае строки в суффиксом s, например, "hello "s, будут представлять объекты *string*.

## Методы класса string

Так как по сути `string` является классом в C++, то у него и его объектов есть куча полезных методов и функций, которые помогают программисту писать красивый, безопасный и поддерживаемый код.

Крайне рекомендовано познакомиться с ними на [самом главном и самом авторитетном онлайн-справочнике по языкам программирования C и C++](https://en.cppreference.com/w/cpp/string/basic_string.html).

Как пример, в коде ниже используются методы string, чтобы получить длину строки. Никаких циклов, итераторов или еще чего-то. Удобно же, согласитесь?
```c++
#include <iostream>
#include <string>
 
int main()
{
    std::string message {"Hello"};
    std::cout << "Length: " << message.length() << std::endl;   // Length: 5
}
```

## Задание

### Описание задачи

Напишите программу на C++, которая демонстрирует работу с `std::string` в различных сценариях. Программа должна выполнять следующие действия (реализованные в виде отдельных функций):

1) **Ввод строки и вывод информации**:
    - Запросить у пользователя ввод строки.
    - Вывести её длину, первый и последний символ, проверить, пуста ли строка.
    - Вывести строку в обратном порядке (помните же, что у объектов string есть методы?).

2) **Поиск и замена**:
    - Запросить подстроку для поиска.
    - Найти все вхождения этой подстроки в исходной строке из задания 1 и вывести их позиции.
    - Заменить все найденные вхождения на другую подстроку (введённую пользователем) и вывести результат.

3) **Разделение строки на слова (токены)**:
    - Разбить строку на слова, используя пробелы
    - Вывести каждое слово в отдельной строке.

4) **Удаление лишних пробелов**:
    - Удалить начальные и конечные пробелы и заменить несколько подряд идущих пробелов внутри строки на один пробел.
    - Вывести результат.

5) **Сравнение строк**:
    - Запросить вторую строку.
    - Сравнить её с исходной лексикографически и вывести, какая строка больше, меньше или они равны.

6) **Преобразование регистра**:
    - Преобразовать исходную строку в верхний и нижний регистр.
    - Вывести оба варианта.

## Требования к реализации
- Программа должна быть написана на C++ (стандарт C++11 или новее).
- Код должен быть разбит на функции, каждая из которых решает одну подзадачу.
- Обязательно использование методов std::string, а не C-строк.
- Предусмотреть обработку ошибок (например, некорректный ввод чисел для позиций).
- Прокомментировать ключевые моменты, особенно там, где используются сложные методы.
