Задача 15. 🔍 

Переменная errno и функция strerror() используются в языке C для обработки ошибок, возникающих при вызове функций стандартной библиотеки и системных вызовов. Переменная errno объявлена в заголовочном файле <errno.h> и представляет собой целочисленный код последней ошибки. Она устанавливается только в случае возникновения ошибки и не сбрасывается автоматически при успешном выполнении функции, поэтому анализировать её значение следует лишь тогда, когда функция явно сигнализирует об ошибке (например, возвращает NULL или -1).

Функция strerror() из заголовочного файла <string.h> принимает код ошибки и возвращает строку с человекочитаемым описанием данной ошибки. 


Скомпилируйте и запустите файл errorcouner. 
gcc errnocounter.c -o errnocounter.out && ./errnocounter.out

Изучите, какому коду ошибки соответствует какая расшифровка. 

Скомпилируйте и запустите файл errorcouner
gcc errno.c -lm -o errno.out && ./errno.out

Объясните, какую функцию выполняет perror? 
