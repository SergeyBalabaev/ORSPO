#include "config.h"
#include "config.h"  
#include <stdio.h>

int main() {
    appConfig.width = 800;
    appConfig.height = 600;
    printf("Width=%d Height=%d\n", appConfig.width, appConfig.height);
    return 0;
}
