Задача 6. 🔍 

Makefile — это файл с инструкциями для утилиты make, которая автоматизирует сборку программ (например, компиляцию исходных файлов в исполняемый файл).
Основной синтаксис

makefile
цель: зависимости
<TAB>команда1
<TAB>команда2

Цель (target) — задача, которую нужно выполнить (например, собрать программу).

Зависимость (prerequisite) — файлы, от которых зависит выполнение цели.

Команды должны обязательно начинаться с символа табуляции (TAB).

Пример простого Makefile

makefile
all: clean
	gcc -c *.c
	gcc *.o -o main

clean:
	rm -f *.o

Как запустить сборку

Для запуска сборки выполните в терминале:

bash
make

По умолчанию выполняется цель all.

Чтобы запустить конкретную цель, укажите её имя:

bash
make all
make clean

Зависимости

Если требуется, чтобы при вызове одной цели перед ней автоматически выполнялась другая (например, очистка старых бинарников перед компиляцией), используйте зависимости.

В примере выше цель all зависит от clean, поэтому при make all сначала сработает очистка.


