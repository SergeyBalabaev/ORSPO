#include <stdio.h>

double progression(int a1, int d, int n)
{
  // Enter your code
}

int main(void)
{
  int a1, d, n;
  scanf("%d %d %d", &a1, &d, &n);
  printf("%.6f\n", progression(a1, d, n));
  return 0;
}
