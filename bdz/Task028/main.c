#include <stdio.h>

int middle_of_three(int a, int b, int c)
{
  // Enter your code
}

int main(void)
{
  int a, b, c;
  scanf("%d %d %d", &a, &b, &c);
  printf("%d\n", middle_of_three(a, b, c));
  return 0;
}
