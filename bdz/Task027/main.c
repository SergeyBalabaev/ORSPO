#include <stdio.h>

const char *get_time_of_day(int n)
{
  // Enter your code
}

int main(void)
{
  int n;
  scanf("%d", &n);
  const char *tod = get_time_of_day(n);
  printf("%s\n", tod);
  return 0;
}
