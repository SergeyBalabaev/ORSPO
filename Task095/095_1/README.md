# Задание 94.3

Ветвление в ассемблере

## Файлы

- `main.s` — исходный код на GAS (AT&T-синтаксис)
- `Makefile` — для сборки программы

## Сборка и запуск

Сборка через Makefile:

```bash
make
```

Запуск:

```bash
./main.out
```

Проверка кода возврата:
```bash
echo $?
```


Модернизируйте программу, чтобы она работала следующим образом:

```c
if(rax==5)
    {
        //Вывод на экран «equal»
    }
        //Выход из программы
```