## Task100 — ELF файл 🔍 

Изучите файл Prog.dmp. 

"Скомпилируйте" программу, использовав скрипт make.sh. Запустите её. 
```bash
sudo apt install xxd -y
chmod +x make.sh
./make.sh
./Prog
```

Измените программу таким образом, чтобы на экран выводились **полностью** ваши ФИО и группа. 