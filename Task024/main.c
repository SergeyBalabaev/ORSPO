#include <stdio.h>

int sum(int a, int b)
{
  // Enter your code
}

int main(void)
{
  int a, b;

  printf("Enter two numbers: ");
  scanf("%d %d", &a, &b);

  int result = sum(a, b);

  printf("Sum: %d\n", result);

  return 0;
}
