# Задание 94.3

Обращение к глобальной переменной по адресу

## Файлы

- `main.s` — исходный код на GAS (AT&T-синтаксис)  
- `Makefile` — для сборки программы

## Сборка и запуск

Сборка через Makefile:

```bash
make
```

Запуск программы:
```
./main
```

Используя команду:
```bash
 objdump -D ./main
 ```
изучите как интерпретируется команда lea 