Задача 10. 🔍 
Для удобства и сокращения кода в Makefile можно использовать переменные:

makefile
CC = gcc
CFLAGS = -Wall -O2

Затем эти переменные можно подставлять в команды, используя синтаксис $(VAR).
Пример Makefile с переменными

makefile
CC := gcc
CFLAGS := -Wall -Wextra

all: clean
	$(CC) -c $(CFLAGS) *.c
	$(CC) *.o -o main

clean:
	rm -f *.o

Запустите скрипт. Убедитесь, что программа собрана. 

Создайте в директории файл с именем clean.

Снова запустите скрипт с целью очистки ненужных файлов. Теперь команда make clean не сработает. 