#include <stdio.h>

??? divide(??? numerator, ??? denominator)
{
  return ///;
}

int main(void)
{
  ??? a, b;
  printf("Enter numerator and denominator: ");
  scanf("%??? %???", &a, &b);

  ??? result = divide(a, b);
  printf("Result: %???\n", result);

  return 0;
}
