#include <stdio.h>

int divide(int numerator, int denominator)
{
  // Enter your code
}

int main(void)
{
  int a, b;
  printf("Enter numerator and denominator: ");
  scanf("%d %d", &a, &b);

  int result = divide(a, b);
  printf("Result: %d\n", result);

  return 0;
}
