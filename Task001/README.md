Задача 1. 🔍 
Скомпилируйте и запустите программу используя команду

gcc main.c -o main
./main

где:
gcc - название компилятора
main.c - название входного файла
-o main - ключ, после которого указывается название выходного файла

Используя команду
gcc -v
определите версию компилятора gcc. 
Под какую архитектуру происходит сборка программы?
Найдите во внешних источниках, какая максимальная допустимая версия языка С может быть скомпилирована с помощью этой версии компилятора? 



<p>Скомпилируйте и запустите программу используя команду</p>

<pre><code>gcc main.c -o main
./main
</code></pre>

<p>где:</p>
<ul>
  <li><code>gcc</code> — название компилятора</li>
  <li><code>main.c</code> — название входного файла</li>
  <li><code>-o main</code> — ключ, после которого указывается название выходного файла</li>
</ul>

<p>Используя команду <code>gcc -v</code> определите версию компилятора gcc.</p>

<p>Под какую архитектуру происходит сборка программы?</p>

<p>Найдите во внешних источниках, какая максимальная допустимая версия языка С может быть скомпилирована с помощью этой версии компилятора?</p>