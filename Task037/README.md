## Задача 037: Факториал числа 3️⃣

### Задание

Написать программу, которая вычисляет факториал целого числа `n`.

- Факториал определяется как:
$
n! = 1 \cdot 2 \cdot 3 \cdot ... \cdot n
$
- Если `n < 0`, вывести `-1`

---

### Прототип функции

```c
unsigned long long factorial(int n);
```
